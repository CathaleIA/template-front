import { requestToRender } from "../../types/typePdfRender"
import { divToBase64 } from "@/utils/htmlTobase";
// vamos a preparar el objeto para la consulta
export async function prepararPDF(objectoBody: requestToRender) {
   

    return(
        <div></div>
    );
};